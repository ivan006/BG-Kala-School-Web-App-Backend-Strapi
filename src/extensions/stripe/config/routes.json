{
  "routes": [
    {
      "method": "POST",
      "path": "/stripe/checkout",
      "handler": "Stripe.checkout"
    },
    {
      "method": "POST",
      "path": "/stripe/webhook",
      "handler": "Stripe.webhook"
    }
  ]
}
